#pragma once

class CapsuleData
{
public:
	float m_length;
	float m_thickness;
	double m_theta;
	CapsuleData(float length, float thickness, double theta) : m_length(length), m_thickness(thickness), m_theta(theta) {}


};